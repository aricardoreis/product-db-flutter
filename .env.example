# API base URL of the product-db NestJS backend.
# Android emulator uses 10.0.2.2 to reach host's localhost.
# Physical device on the same Wi-Fi: use the host's LAN IP (e.g. 192.168.x.x:3000).
API_BASE_URL=http://10.0.2.2:3000
